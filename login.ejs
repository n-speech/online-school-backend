<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Вход</title>
  <style>
    :root {
      font-size: 16px;
    }

    body {
      margin: 0;
      background: #f9f9f9;
      font-family: Arial, sans-serif;
      line-height: 1.5;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      height: 100vh;
      padding-top: 1em;
    }

    .login-box {
      position: relative;
      background: white;
      border: 1px solid #ddd;
      border-radius: 0.375em;
      padding: 2em 2.5em;
      box-shadow: 0 0.125em 0.5em rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 40%; /* Узкая форма */
      text-align: center;
      box-sizing: border-box;
      margin: 0 auto; /* Центрирование */
    }

    h1 {
      margin-top: 0;
      font-weight: normal;
      color: #222;
      font-size: 1.6rem;
    }

    .close-button {
      position: absolute;
      top: 1rem;
      left: 1rem;
      font-size: 2rem;
      text-decoration: none;
      color: #999;
      transition: color 0.3s ease;
      z-index: 10;
    }

    .close-button:hover {
      color: #FFD966;
    }

    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 0.75em 1em;
      margin-bottom: 0.9em;
      border: 1px solid #ccc;
      border-radius: 0.25em;
      font-size: 1rem;
      box-sizing: border-box;
    }

    button#btnLogin {
      width: 100%;
      padding: 0.5em 0;
      background-color: #FFD966;
      color: #241c15;
      border: none;
      border-radius: 0.3em;
      font-size: 1.3rem;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s ease;
    }

    button#btnLogin:hover {
       background-color: #FCE27E;
    }

    #message {
      margin-top: 1.1rem;
      color: red;
      min-height: 1.25em;
      font-weight: 600;
      font-size: 0.9rem;
    }

    /* Планшеты и горизонтальные телефоны */
    @media (max-width: 768px) {
      body {
        padding-top: 1em;
      }

      .login-box {
        width: 90%;
        max-width: 90%;
        padding: 2em 1em;
        margin: 0 auto;
        border-radius: 0.5em;
        box-shadow: 0 0.125em 0.5em rgba(0,0,0,0.1);
      }

      h1 {
        font-size: 1.5rem;
      }

      input[type="email"],
      input[type="password"],
      button#btnLogin {
        font-size: 1.2rem;
      }
    }

    /* Мобильные телефоны */
    @media (max-width: 480px) {
      body {
        padding-top: 2em;
      }

      .login-box {
        width: 90%;
        max-width: 320px;
        padding: 1.5em 1em;
        margin: 0 auto;
        border-radius: 0.5em;
        box-shadow: 0 0.125em 0.5em rgba(0,0,0,0.1);
      }

      h1 {
        font-size: 1.6rem;
        margin-bottom: 1em;
      }

      input[type="email"],
      input[type="password"],
      button#btnLogin {
        font-size: 1.2rem;
      }
    }
  </style>
</head>
<body>
  <div class="login-box"> 
    <h1>Login form</h1>

    <% if (error) { %><p style="color:red;"><%= error %></p><% } %>
    <form action="/login" method="POST">
   
   <a href="#" class="close-button" aria-label="Закрыть" onclick="window.history.back()">×</a> 



      <input type="email" id="email" name="email" placeholder="Email" required>
      <input type="password" id="password" name="password" placeholder="Password" required>
      <button id="btnLogin" type="submit">Log in</button>
    </form>
    <p id="message"></p>
  </div>
</body>
</html>
